# Web / API
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# Vector store
chromadb==1.5.9

# Local embeddings: pure-Python TF-IDF (see app/rag/embeddings.py). No
# PyTorch/sentence-transformers dependency -- that approach was replaced
# because loading PyTorch inside the MCP server subprocess reliably exceeded
# Render free tier's 512MB memory cap (see deployed.md). TF-IDF needs no
# extra packages beyond the stdlib.

# Anthropic Claude SDK (LLM calls; reads ANTHROPIC_API_KEY from env at runtime)
anthropic==0.120.2

# MCP (Model Context Protocol) server + client, stdio transport.
# Pinned to the 1.x line (FastMCP high-level API + mcp.client.stdio /
# mcp.client.session.ClientSession) rather than the newer 2.0.0 major version,
# for API stability and documentation coverage.
mcp==1.29.0
# Required for httpx to work through a SOCKS proxy in some sandboxed network
# environments (harmless extra dependency elsewhere).
socksio==1.0.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0

# HTTP client (used by FastAPI TestClient, demo scripts, anthropic, mcp)
httpx==0.28.1

# Config
python-dotenv==1.2.2
pydantic==2.13.4

# Corpus ingestion (markdown / HTML parsing)
markdown-it-py==4.2.0
beautifulsoup4==4.15.0
